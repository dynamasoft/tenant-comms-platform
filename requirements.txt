# Tenant Communication Platform — pip install path.
# Mirrors the runtime dependencies in pyproject.toml (the `uv` path).
# Usage:  pip install -r requirements.txt
#
# Note: the project also works with `uv sync` (recommended). This file exists so the
# demo runs with plain pip per the capstone spec.

# --- ADK 2.0 + Google GenAI / Vertex ---
google-adk[gcp]>=2.0.0,<3.0.0
google-cloud-aiplatform>=1.156.0

# --- Model Context Protocol (Florida law MCP server + client) ---
mcp>=1.2.0

# --- Streamlit UI ---
streamlit>=1.40.0

# --- Document loading / extraction ---
pypdf>=5.1.0
python-docx>=1.1.2

# --- Config ---
python-dotenv>=1.0.1

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=1.0.0
